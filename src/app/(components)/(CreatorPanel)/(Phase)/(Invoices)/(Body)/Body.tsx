'use client'

import NumberInput from "../../(CreatorInputs)/(numberInput)/numberInput"

const Body = () => {
    return (
        <>
            <NumberInput label="How many contractors?" dispatched={() => (null)} />
        </>
    );
};

export default Body;